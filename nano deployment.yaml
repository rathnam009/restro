apiVersion: apps/v1
kind: Deployment
metadata:
  name: car-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: car-app
  template:
    metadata:
      labels:
        app: car-app
    spec:
      containers:
      - name: car-app
        image: YOUR_ECR_URI:latest
        ports:
        - containerPort: 80
